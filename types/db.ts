export type Nullable<T> = T | null

export enum BucketType {
  avatar = 'avatar',
  cover = 'cover',
}

export enum UserField {
  username = 'username',
  name = 'name',
  // detail
  avatar = 'avatar',
  about = 'about',
  covers = 'covers',
  links = 'links',
}
